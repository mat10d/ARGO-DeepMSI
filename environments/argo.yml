name: argo
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pip
  - pip:
    # Install argo-deepmsi package in editable mode
    # This allows development while having the package importable
    - -e ..