name: argo
channels:
  - pytorch
  - nvidia
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - pytorch>=2.0
  - torchvision
  - pytorch-cuda=12.1
  - pip
  - pip:
    # Install argo-deepmsi package with all dependencies
    - -e ..[dev]
